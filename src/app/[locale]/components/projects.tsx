import React from 'react';

import { Box, Typography } from '@/common/components/components';

const Projects: React.FC = () => {
  return (
    <Box>
      <Typography variant="h1">Projects</Typography>
    </Box>
  );
};

export { Projects };
